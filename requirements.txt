# 1.34.0 is the floor because scan mode asks the server for its unscanned
# backlog: iter_posts(sentinel_scanned=False). Older SDKs do not accept that
# keyword, so an older pin is not "compatible", it is a TypeError on the first
# scan. (1.32.0 was the previous floor, for the two-step registration flow —
# register_begin / register_confirm — which is still required.)
#
# The <2 ceiling stays: 2.0.0 was yanked from PyPI, so 1.x is the live line.
colony-sdk>=1.34.0,<2
requests>=2.31.0
