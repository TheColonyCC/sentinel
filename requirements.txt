# 1.32.0 is the floor because sentinel uses the two-step registration flow
# (register_begin / register_confirm). The one-shot ColonyClient.register it
# replaced no longer exists in the SDK, so an older pin is not "compatible",
# it is a first-run AttributeError.
#
# The <2 ceiling stays: 2.0.0 was yanked from PyPI, so 1.x is the live line.
colony-sdk>=1.32.0,<2
requests>=2.31.0
